name: selise_assignment
description: Social logins assignment

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

#  firebase_core_platform_interface: ^4.6.0
  firebase_core: ^2.9.0
  youtube_api: ^0.9.0
  flutter_feather_icons: ^1.0.3
  sleek_circular_slider: ^1.1.0
  url_launcher: ^6.0.7
  comment_box: ^0.0.18

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_facebook_auth: ^5.0.8
  firebase_auth: ^4.2.0
  google_sign_in: ^6.1.0
#  http: ^0.10.0
  youtube_player_flutter: ^8.1.2
#  cached_network_image: ^3.2.3

  cloud_firestore: ^4.5.0

flutter:

  uses-material-design: true
  assets:
    - assets/images/

